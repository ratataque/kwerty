---
title: "tmux"
description: "Terminal multiplexer with vim-style navigation and session persistence"
category: "terminal"
tool: "tmux"
contributors: ["ratataque", "GrimalDev"]
repos: ["ratataque/.dotfiles", "GrimalDev/dotfiles"]
configPath: "tmux/tmux.conf"
platforms: ["linux", "macos"]
tags: ["terminal", "multiplexer"]
---

## Overview

tmux is the foundation of the terminal workflow. It provides persistent sessions, window splits, and session management. The config uses `C-a` as prefix (remapped from default `C-b`) and vim-style pane navigation.

## Core Configuration

```bash
# Prefix
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# True color support
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"

# Mouse support
set -g mouse on

# Start windows and panes at 1
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on
```

## Vim-Style Navigation

```bash
# Pane navigation
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Pane resizing
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# Split panes
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
```

## Copy Mode (Vi)

```bash
set-window-option -g mode-keys vi
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel
bind -T copy-mode-vi C-v send-keys -X rectangle-toggle
```

## Status Bar

The tmux status bar mirrors the Catppuccin Mocha theme:

```bash
# Status bar
set -g status-position top
set -g status-style "bg=#1e1e2e,fg=#cdd6f4"

# Left
set -g status-left "#[bg=#a6e3a1,fg=#1e1e2e,bold] #S #[bg=#1e1e2e]"
set -g status-left-length 40

# Right
set -g status-right "#[fg=#a6adc8]%H:%M #[fg=#45475a]| #[fg=#89b4fa]%d-%b"

# Window
set -g window-status-format "#[fg=#6c7086] #I:#W"
set -g window-status-current-format "#[fg=#a6e3a1,bold] #I:#W"
```

## Session Management with sesh

tmux sessions are managed via sesh for fast project switching. See the sesh config for details.
